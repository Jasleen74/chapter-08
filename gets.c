#include<stdio.h>
int main() {
      char string[30];

      gets(string);

     // printf("Your name is : %s", string);
       puts(string);     // it pust the cursor in the next lines which simply means a \n 
 
       printf("Oo hi : %s", string);
      

  
 return 0;
}  